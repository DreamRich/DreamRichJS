const button = {
  raisedButton: {
    primaryColor: '#00D0A7',
    primaryTextColor: '#fff',
  },
  flatButton: {
    primaryTextColor: '#00D0A7',
  },
};

var data = {};

Object.assign(data, button);

export default { data };
